<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>{{ page_title }}</title>
    <!-- css plugins-->
    {{function('wp_head')}}
</head>
<body>
<header class="site_header">
    <div class="container">
        <nav class="navbar navbar-expand-lg fixed-top">
            <div class="container">
            <a class="navbar-brand ml-5" href="{{ site.link }}"> <img src="{{ theme.link }}/assets/img/logo.svg" alt="logo"></a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><img src="{{ theme.link }}/assets/img/menu.svg" alt="menu"></button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    {# <li class="nav-item active plr_1"><a class="nav-link text-tc font-bold" href="#">مقارنة</a></li> #}
                    {# <li class="nav-item active plr_1"><a class="nav-link text-tc font-bold" href="#">كوبونات خصم</a></li> #}
                    <li class="nav-item active plr_1"><a class="nav-link text-tc font-bold" href="{{ site.link }}#services">الخدمات</a></li>
                    <li class="nav-item active plr_1"><a class="nav-link text-tc font-bold" href="{{ site.link }}#market">السوق</a></li>
                </ul>
            </div>
            <div class="userInfo mr-auto">
                <ul class="d-flex align-items-center">
                    {% if user %}
                        {% set user_id = function('get_current_user_id') %}
                    <li class="border-left border-right plr_1"> <a class="text-tc font-bold d-inline-flex align-items-center" href="{{ function('get_permalink', function('get_page_by_path', 'profile')) }}">حسابي
                            {% if fn('wp_is_mobile') %}
                            {% else %}
                            <img class="ms-1 header_profile_picture" src="{{ user_id|get_user_profile_pic }}" alt="icon">
                            {% endif %}
                        </a></li>
                    {% else %}

            {% endif %}
                    {% if user %}
                        <li class="pr_1"> <a class="red_btn" href="{{ fn('wp_logout_url',site.link) }}" style="font-size:.8rem">تسجيل خروج</a></li>
                    {% else %}
                        <li class="pr_1"> <a class="red_btn" href="{{ function('get_permalink', function('get_page_by_path', 'login')) }}" style="font-size:.8rem">تسجيل حساب / الدخول</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
        </nav>
    </div>
    <button class="how_to_add_ads ads bg-red text-white text-center ptb_1 plr_1 d-flex align-items-center justify-content-center border-0 w-100" type="button" data-bs-toggle="modal" data-bs-target="#addModal"><span class="font-regular me-2"> طريقة إضافه  </span><span class="font-bold"> اعلان مجاني</span><img class="ms-2" src="{{ theme.link }}/assets/img/info.svg" alt="info"></button>
</header>
<div class="modal fade" id="addModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true" style="margin:auto;left:0">
    <div class="modal-dialog w-60 mw-100">
        <div class="modal-content w-100">
            <div class="modal-body w-100">
{#                <iframe id="_video"  class="w-100 height-500" src="https://www.youtube.com/embed/38y_1EWIE9I" title="YouTube video player" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>#}
                <h2 style="text-align: center">قريباً..</h2>
            </div>
        </div>
    </div>
</div>